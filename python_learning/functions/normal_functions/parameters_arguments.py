# perform addition
def addition(a, b): # a and b are parameters
    print(f"value of a : {a}")
    print(f"value of b : {b}")

    result = a + b
    print(f"result : {result}")

addition(10,10) # paasing values for a and b are called as arguments